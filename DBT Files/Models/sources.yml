version: 2

sources:
  - name: raw
    description: Raw sales data from CSV files
    schema: main
    tables:
      - name: sales_orders_day_one
        description: Raw sales orders data
        config:
          meta:
            external_location: "read_csv_auto('data/SalesOrders_Day_One.csv', header=true)"
